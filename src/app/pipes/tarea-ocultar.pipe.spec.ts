import { TareaOcultarPipe } from './tarea-ocultar.pipe';

describe('TareaOcultarPipe', () => {
  it('create an instance', () => {
    const pipe = new TareaOcultarPipe();
    expect(pipe).toBeTruthy();
  });
});
